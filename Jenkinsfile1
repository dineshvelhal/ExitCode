node {
    stage('EXIT_CODE_1') {
		def executionStatus = {
			 "java -jar ExitCode 1".execute().exitValue()
		}
		
		int code = executionStatus()
		
		if (code != 0) {
			error "Pipeline aborted due to MAGiX quality gate failure. Code: ${code}"
		}
    }
}